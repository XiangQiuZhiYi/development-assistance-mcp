# 核心必读示例

这是一个 `CORE_GUIDELINES.md` 的示例文件，展示如何设置强制性开发规范。

## 📝 使用方式

在 AI 对话中说：

```
"设置核心必读内容"
```

然后提供以下内容：

---

## 示例 1：Vue 项目完全使用 TSX 模式

```markdown
## 🚫 禁止事项

### 文件格式
- ❌ **禁止使用 .vue 文件**
- ✅ 所有组件必须使用 .tsx 文件

### 组件编写
- ❌ **禁止使用 v-model 控制弹窗**
- ✅ 所有弹窗组件必须通过 handleOpen/handleClose 方法控制

### 类型定义
- ❌ **禁止使用 any 类型**
- ✅ 必须明确定义所有类型

## ✅ 强制规范

### 命名规范
- 组件名称：PascalCase（如：UserDialog.tsx）
- 函数名称：camelCase（如：handleUserClick）
- 常量名称：UPPER_SNAKE_CASE（如：API_BASE_URL）

### 组件结构
```tsx
// ✅ 正确示例
export const UserDialog = () => {
  const [open, setOpen] = useState(false);
  
  const handleOpen = () => setOpen(true);
  const handleClose = () => setOpen(false);
  
  return (
    <Dialog open={open} onClose={handleClose}>
      {/* 内容 */}
    </Dialog>
  );
};

// ❌ 错误示例 - 不要这样写
<template>
  <el-dialog v-model="visible">
    <!-- 内容 -->
  </el-dialog>
</template>
```

### 双语规范
- 所有用户可见文本必须支持中英文
- 使用 i18n 进行国际化

## 🔒 安全规范

- 禁止使用 eval() 和 Function() 构造函数
- 所有用户输入必须经过验证和转义
- 敏感信息（API Key、密码等）不得硬编码
```

---

## 示例 2：React + TypeScript 项目

```markdown
## 🚫 禁止事项

- ❌ 禁止使用 class 组件，统一使用函数组件
- ❌ 禁止使用 default export，统一使用 named export
- ❌ 禁止在组件内定义内联函数（性能优化）
- ❌ 禁止直接修改 state，必须使用 setState

## ✅ 强制规范

### 组件定义
```tsx
// ✅ 正确
export const UserCard: React.FC<UserCardProps> = ({ user }) => {
  return <div>{user.name}</div>;
};

// ❌ 错误
export default function UserCard(props) { ... }
```

### Hooks 使用
- 必须遵循 Hooks 规则
- 自定义 Hooks 必须以 use 开头
- useEffect 必须声明所有依赖项

### 状态管理
- 使用 Zustand 进行全局状态管理
- store 文件统一放在 src/store/ 目录
- 命名规范：useXxxStore

### 样式规范
- 使用 Tailwind CSS
- 禁止使用内联样式
- 必要时使用 CSS Modules
```

---

## 示例 3：Next.js App Router 项目

```markdown
## 🚫 禁止事项

- ❌ 禁止使用 Pages Router
- ❌ 禁止在 Server Component 中使用 useState/useEffect
- ❌ 禁止在 Client Component 中执行数据库查询

## ✅ 强制规范

### 文件组织
```
app/
├── (auth)/          # 路由组
│   ├── login/
│   └── register/
├── dashboard/
│   ├── page.tsx     # 页面
│   └── layout.tsx   # 布局
└── api/            # API 路由
```

### 组件分类
- Server Component（默认）：用于数据获取和 SEO
- Client Component（'use client'）：用于交互逻辑

### 数据获取
```tsx
// ✅ Server Component
export default async function UserPage() {
  const users = await db.user.findMany();
  return <UserList users={users} />;
}

// ✅ Client Component
'use client';
export const UserList = ({ users }) => {
  const [filter, setFilter] = useState('');
  return ...
}
```

### Metadata
- 每个页面必须定义 metadata
- 必须包含 title 和 description
```

---

## 💡 使用建议

1. **根据项目实际情况编写**：不要照搬示例，要根据你的项目特点定制
2. **保持简洁明确**：规范要清晰，避免模糊不清的描述
3. **提供示例代码**：用 ✅ 和 ❌ 对比正确和错误的写法
4. **定期更新**：随着项目发展，及时更新核心规范
5. **团队共识**：确保团队成员都理解和认可这些规范

## 🎯 核心必读 vs 项目指南

| 文档 | 内容类型 | 示例 |
|------|---------|------|
| CORE_GUIDELINES.md | 强制性规范，绝不可违反 | 禁止使用 .vue 文件 |
| PROJECT_GUIDE.md | 项目信息和建议性规范 | 框架：Vue 3 + Vite |
| PERSONAL_DEV_NOTES.md | 代码片段和技巧 | 优质的 Toast 组件 |

**记住**：核心必读是 AI 的"红线"，必须严格遵守！
